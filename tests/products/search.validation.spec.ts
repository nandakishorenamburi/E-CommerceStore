import { test } from '@playwright/test';

test.describe(
  'Products — search validation @products',
  () => {

    test.skip(
      'search results contain only products matching keyword @regression @products',
      async () => {}
    );

    test.skip(
      'search with no results shows empty state message @regression @products',
      async () => {}
    );

    test.skip(
      'search is case insensitive @regression @products',
      async () => {}
    );

});