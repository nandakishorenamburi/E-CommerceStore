import { test } from '@playwright/test';

test.describe(
  'Orders — order history management @orders',
  () => {

    test.skip(
      'order history shows most recent order first @regression @orders',
      async () => {}
    );

    test.skip(
      'order detail shows correct product and price @regression @orders',
      async () => {}
    );

});